name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Ruff
        run: ruff check app tests
      - name: Built assets are current
        run: python scripts/build_static.py --check
      - name: Alembic upgrade smoke test
        env:
          DATABASE_URL: sqlite:////tmp/luminos_engine_ci_migration.db
        run: alembic upgrade head
      - name: Tests
        env:
          DATABASE_URL: sqlite:////tmp/luminos_engine_ci.db
          LUMINOS_AUTH_REQUIRED: "false"
          LUMINOS_ADMIN_SECRET: ci-admin-secret
          LUMINOS_ENFORCE_ADMIN_SECRET: "false"
        run: pytest
